# Grafana declarative datasource provisioning - wires up the Prometheus
# datasource with zero manual UI setup on first boot. See CLAUDE.md's
# Telemetry section.
apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: false
